[package]
name = "cli"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
disguise = { path = "../../core/lib" }

[build-dependencies]
disguise = { path = "../../core/lib", features = ["pretty"] }
#disguise = { path = "../../core/lib" }
